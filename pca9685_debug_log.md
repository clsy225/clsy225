# PCA9685 Servo Debug Log

## 2026-05-14 16:26 BST
- 用户反馈：`V+` 实测约 5V。
- 用户反馈：接线正常。
- 用户反馈：舵机“刚刚能跑一下”，之前抖过，现在又不抖。
- 当前判断：问题更像硬件稳定性 / 接触 / 带载波动，而不是单纯前端逻辑错误。

## 当前已确认
- I2C 可见 `0x40` 和 `0x70`
- PCA9685 寄存器可读写
- `MODE1=0x21`
- `PRESCALE=0x79`（50Hz 合理）
- Flask 服务在运行，前端请求可到后端
- `pca9685_servo_test.py` 可成功写通道 0 的角度序列
- `pca9685_single_servo_diagnostic.py` 已新增并验证可写入通道 0 的 PWM 寄存器

## 本轮计划
1. 给页面控制做小幅增强，不大改结构
2. 增加更清晰的页面状态提示
3. 支持“仅通道0”单通道测试模式，便于最小化验证
4. 对滑块/连续控制做节流，减少 I2C 压力
5. 每次代码改动同步更新本日志

## 2026-05-14 16:31 BST
- 新增仓库日志文件：`pca9685_debug_log.md`
- 页面增强：加入页面版本号，便于确认浏览器是否加载到新页面
- 页面增强：加入“单舵机诊断模式（只显示当前选中通道）”
- 页面增强：加入自动刷新开关
- 页面增强：滑块改为节流发送，避免拖动时短时间大量 I2C 写入
- 页面增强：发送中状态提示
- 后端增强：统一异常处理，接口报错时返回 JSON 并更新 `last_error`

## 2026-05-14 16:32 BST
- 按用户要求，主交互改回“5 个舵机 + 键盘/命令行调试”，不再把单舵机模式当成默认使用方式
- 增强 `pca9685_keyboard_control.py`：更清楚的 5 舵机交互、更好的状态输出、更适合现场调试
- 新增当前活动通道概念，默认选中第一个通道
- 新增命令：`select` / `current` / `left` / `right` / `up` / `down` / `nudge` / `step-size` / `big-step` / `diag` / `status`
- 提升输出：每次动作打印时间、角度、pulse(us)、PWM counts，方便判断命令是否真正下发
- 保留 Flask 页面作为辅助，不再继续大改页面结构

## 2026-05-14 16:37 BST
- 用户反馈：页面已能控制角度，但“哪怕不动整个舵机都会抖”
- 判断：这更像保持脉冲导致的静止抖动，以及默认脉宽范围过宽，不像页面连接问题
- Flask 默认脉宽从 `500~2500us` 收窄为 `1000~2000us`
- 页面新增“空闲后自动释放 PWM”，默认开启，延时约 400ms 自动 release 当前通道
- 目标：减少静止时持续保持脉冲带来的抖动

## 2026-05-14 16:40 BST
- 用户反馈：点击“释放全部”后舵机都不动了，但至少不抖
- 结论更明确：抖动主要来自持续保持脉冲；release 后安静，但失去主动保持
- 页面新增“自动释放延时”可调：100 / 200 / 400 / 800 / 1500 ms
- 给“释放全部 PWM”增加确认提示，避免误触后以为页面坏了
- 页面版本更新为 `2026-05-14-1640`

## 2026-05-14 16:53 BST
- 用户反馈：点了之后“直接全部不动了”
- 排查结果：前端/后端原先只记录“目标角度”，没有记录“当前通道是否已经 release”，导致页面看起来还在某个角度，但硬件其实已释放，容易误判为“全部不动”
- 新增每个通道的 `released` 状态
- 页面卡片上明确显示“当前已释放，不保持”
- “释放全部 PWM”按钮改名为“诊断：释放全部 PWM”，降低误操作概率
- 页面版本更新为 `2026-05-14-1653`

## 2026-05-14 17:00 BST
- 用户明确指出：原来第一代页面代码能动，问题是我后续为“消抖”加入的自动 release / 状态逻辑把可动基线搞坏了
- 决策：先回滚到“能动的第一代页面逻辑”，停止继续叠加自动释放策略
- Flask 页面回到简单基线：无自动释放、无自动释放延时、无 released 状态提示
- 保留最基本的版本号与状态显示，版本设为 `2026-05-14-1700-baseline`
- 目标：先恢复 5 通道角度控制的可用性，再单独设计不破坏控制链路的消抖方案

## 2026-05-14 17:06 BST
- 用户确认：基线版“确实能动”，但 release 后“什么都动不了”；同时 release 确实能消除抖动
- 结论：自动/全局 release 不是合适方案，但“只对当前通道手动停止抖动”是有价值的
- 页面改成折中方案：恢复基线可动逻辑，不做自动 release
- 单通道按钮从“释放”改成更贴近意图的“停止抖动”
- 全局按钮改成“诊断：释放全部 PWM”，并加入强提示，避免当正常控制使用
- 页面版本更新为 `2026-05-14-1706-hybrid`

## 2026-05-14 17:12 BST
- 用户同意先做“安全节流消抖”
- 不再碰自动 release，仅减少滑块拖动时的高频重复写入
- 页面滑块由直接 `setAngle()` 改为 `queueAngle()`，约 120ms 节流后只发送较新的目标值
- 目标：降低持续重复写入导致的抖动，同时不破坏“设角度就能动”的基线逻辑
- 页面版本更新为 `2026-05-14-1712-safe-throttle`

## 2026-05-14 17:20 BST
- 用户认可借鉴外部示例的关键思路：`oninput` 只更新显示，`onchange` 松手才真正发送舵机命令
- 页面滑块改为“拖动只预览，松手才发送”
- 新增 `previewAngle()` 与 `commitAngle()`，移除节流队列逻辑
- 目标：进一步减少高频命令写入，让“不操作时不要抖”更接近硬件自然状态
- 页面版本更新为 `2026-05-14-1720-change-only`

## 2026-05-14 17:24 BST
- 用户反馈：点击“全部回中”后全部不动
- 日志确认：`POST /api/center_all` 返回了 500，而后续单通道 `set_angle` 仍是 200，说明不是整站死掉，而是“全部回中”这条批量路径本身失败
- 调整 `center_all()`：逐通道容错，单个通道失败不再让整次回中直接 500
- 若部分通道失败，页面 `last_error` 会保留失败通道信息，便于继续定位是哪个通道/I2C 写入出问题

## 2026-05-14 17:26 BST
- 用户反馈：抖动仍存在，希望重启后继续写
- 新方案：不做自动 release，改为“轻保持消抖（soft hold）”
- 思路：停止抖动时，不是把 PWM 彻底放掉，而是用更保守的脉宽映射（`1000~2000us`）重新保持当前角度
- 页面新增“轻保持消抖”开关，默认开启
- 单通道“停止抖动”在开启轻保持时会调用 `/api/park`，关闭时仍走 `/api/release`
- 目标：尽量兼顾“还能继续控”和“静止时别抖得太厉害”

## 2026-05-14 17:34 BST
- 用户同意直接借鉴开源机械臂常见做法：每通道单独限幅，而不是所有舵机统一 0~180
- 新增 `CHANNEL_LIMITS`，当前采用保守初值：
  - ch0: 10~170
  - ch1: 55~170
  - ch2: 10~170
  - ch3: 10~170
  - ch4: 45~100
- 前端滑块范围改为读取每通道 `min_angle/max_angle`
- 后端 `set_angle()` 与 `park_angle()` 也按通道限幅，避免页面绕过限制
- 页面版本更新为 `2026-05-14-1734-channel-limits`

## 2026-05-14 17:40 BST
- 用户同意继续做“每通道脉宽校准版”，并要求记得上传 git
- 新增 `CHANNEL_PULSE_US`，为 5 个通道分别配置 `min_us/max_us` 与 `park_min_us/park_max_us`
- 当前保守初值：
  - ch0: 900~2100, park 1000~2000
  - ch1: 1000~1950, park 1050~1900
  - ch2: 950~2050, park 1000~1950
  - ch3: 950~2050, park 1000~1950
  - ch4: 1100~1900, park 1150~1850
- 保留 1734 的每通道角度限幅
- 保留“松手才发送”与“停止抖动/轻保持消抖”
- 页面版本更新为 `2026-05-14-1740-channel-calibration`

## 2026-05-14 17:54 BST
- 用户先去处理 git，要求我继续调舵机
- 新增前端 deadband：默认 `2°`
- 规则：如果新目标角度与当前角度差值小于 `2°`，则不发送新命令
- 目标：抑制目标位附近的微小来回修正，减少“89/90/89/90”式抖动
- 页面版本更新为 `2026-05-14-1754-deadband`

## 2026-05-14 18:05 BST
- 用户反馈：“停止抖动没用”
- 日志定位到关键问题：前端在短时间内连续多次调用 `/api/park`，不是一次性止抖，而是在反复重写同一通道
- 修复 `stopJitter()`：加入 `busy` 防重入锁，避免按钮重复触发时连续狂发 park/release 请求
- 页面版本更新为 `2026-05-14-1805-stop-jitter-fix`
